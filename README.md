# Photo Editor — CLI для массовой обработки изображений

## Установка зависимостей

```bash
pip install -r requirements.txt
```

---

## Примеры использования

### Использование из исходников (Python)

Уменьшить все фото до max-side 1200px и конвертировать в WebP:

```bash
python photo_editor.py --input ./photos --output ./out --max-side 1200 --format webp --quality 80
```

### Ресайз до 800×600 с обрезкой по центру, формат JPEG

```bash
python photo_editor.py -i ./raw -o ./processed --width 800 --height 600 --crop-center --format jpeg
```

### Только изменить ширину (высота пропорционально)

```bash
python photo_editor.py -i ./input -o ./output --width 1024
```

### Конвертировать всё в PNG без ресайза

```bash
python photo_editor.py -i ./input -o ./output --format png
```

### Кастомное именование файлов

```bash
python photo_editor.py -i ./photos -o ./icons --name logo --format webp --max-side 256
```

Результат: `logo-1.webp`, `logo-2.webp`, `logo-3.webp`, …

---

## Сборка в standalone бинарник (PyInstaller)

### Быстрая сборка через build.sh (Mac)

```bash
chmod +x build.sh
./build.sh
```

Бинарник → `production/photo_editor`

Запуск:

```bash
./production/photo_editor -i ./photos -o ./out --max-side 1200 --format webp
```

### Ручная сборка для macOS

```bash
pip install pyinstaller
pyinstaller --onefile --name photo_editor photo_editor.py
mkdir -p production && cp dist/photo_editor production/
```

### Сборка для Windows (`.exe`)

Если вы (или заказчик) на Windows, просто дважды кликните по файлу `build.bat` (или запустите его через командную строку):

```cmd
build.bat
```

Скрипт сам установит PyInstaller (если нужно) и бережно соберёт в папке `production/` все `.exe` файлы:
- `photo_editor.exe`
- `make-370x370.exe`
- `make-1920x398.exe`

---

## 🚀 Готовые бинарники (production/)

После запуска `build.sh`, в папке `production/` появятся **готовые исполняемые файлы**, которые работают без установки Python и зависимостей.

### 1. Основной скрипт (`photo_editor`)
Это полнофункциональная CLI-тулза.

**Использование:**
```bash
./production/photo_editor -i ./photos -o ./out --max-side 1200 --format webp --quality 80
```

### 2. Автономные пресеты (`make-370x370`, `make-1920x398`)
Это специальные самостоятельные файлы "всё-в-одном", идеальные для передачи заказчику.
Они не требуют аргументов и сами спрашивают базовое имя.

**Как пользоваться:**
1. Положите файл (например, `make-370x370`) прямо в папку с вашими фотографиями.
2. Запустите его (двойным кликом или через терминал ` ./make-370x370 `).
3. Введите префикс (например: `product`).
4. Рядом автоматически появится папка `product_370x370/` с готовыми WebP-фотографиями нужного размера!

### Как добавить свой пресет?
Добавить новые размеры очень просто прямо в коде:
1. Зайдите в папку `presets/`.
2. Скопируйте существующий файл (например, `make_370x370.py`) и назовите его `make_800x600.py`.
3. Отредактируйте параметры (WIDTH, HEIGHT, FORMAT, QUALITY) внутри нового файла.
4. Снова запустите `./build.sh` — PyInstaller автоматически соберёт ваш новый бинарник `make-800x600` в папке `production/`!

---

## ☁️ Автоматическая сборка в облаке (GitHub Actions)

В проекте настроен CI/CD пайплайн (`.github/workflows/build.yml`), который умеет **сам собирать бинарники для Windows, macOS и Linux**! 

Если вы загрузите этот проект на GitHub, то каждый раз, когда вы вносите изменения (например, добавляете новый пресет), GitHub автоматически:

1. Поднимет серверы с Windows, macOS и Linux.
2. Скачает Python, установит зависимости и всё соберёт. 
3. Сформирует 3 готовых `ZIP`-архива со всеми вашими бинарниками внутри!

**Где скачать архивы?**
Зайдите в репозиторий на GitHub → Вкладка **Actions** → Клик по последнему зелёному запуску "Build Binaries" → В самом низу страницы под словом `Artifacts` вы увидите:
- `photo-editor-binaries-Windows`
- `photo-editor-binaries-macOS`
- `photo-editor-binaries-Linux`

Скачивайте нужный архив и внутри будут готовые `.exe` или бинарники!


---

## Поддерживаемые форматы

| Вход             | Выход              |
| ---------------- | ------------------ |
| `.jpg` `.jpeg`   | `jpeg`             |
| `.png`           | `png`              |
| `.webp`          | `webp`             |
