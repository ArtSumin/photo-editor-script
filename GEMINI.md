Ты — senior Python developer и DevOps-инженер.

Мне нужна production-ready CLI программа для массовой обработки изображений.

Требования:

1. Программа должна быть написана на Python.
2. Должна запускаться из командной строки.
3. Должна принимать аргументы через argparse.
4. Должна уметь:

   - Принимать входную папку (--input)
   - Принимать выходную папку (--output)
   - Изменять размер:
        --width (опционально)
        --height (опционально)
        --max-side (опционально)
   - Менять формат:
        --format (jpeg, png, webp)
   - Устанавливать качество:
        --quality (0-100)
   - Опционально обрезать изображение по центру (--crop-center флаг)

5. Должна обрабатывать все изображения в папке:
   jpg, jpeg, png, webp

6. Должна:
   - Сохранять пропорции если указана только ширина или только высота
   - Использовать Pillow
   - Корректно конвертировать в RGB для JPEG
   - Создавать output папку если её нет
   - Выводить прогресс в консоль

7. Код должен быть:
   - Чистый
   - Разбит на функции
   - С main() точкой входа
   - С if __name__ == "__main__"

8. После написания:
   - Покажи пример запуска из терминала
   - Напиши инструкцию как собрать в standalone бинарник через PyInstaller
   - Команду для Mac
   - Команду для Windows

9. Не объясняй теорию. 
   Дай:
   - Полный код файла
   - Команду установки зависимостей
   - Команду сборки
   - Пример использования

Программа должна быть пригодна для реального использования.